# 智批云 前端任务清单

> **技术栈**：Vue 3 + Vite + Vue Router + Axios + Chart.js + vue-chartjs  
> **已有**：8 个完成组件 + 2 个部分完成组件（共 10 个视图）  
> **标注**：🔴 高优先级  🟡 中优先级  🟢 低优先级  ✅ 已完成

---

## 一、教师端 — 试卷管理（🔴 核心缺失）

### ✅ 1.1 试卷列表展示
- 表格：名称/科目/班级/总分/日期/状态
- 状态：已完成

### ✅ 1.2 创建试卷
- 模态框：标题/科目/总分/班级/日期/描述
- 状态：已完成

### 🔴 1.3 标准答案录入（关键缺失）
```
在 PapersView.vue 中新增"录入答案"功能
```
- 在试卷列表中增加"录入答案"按钮
- 弹出模态框，逐题录入客观题答案（A/B/C/D）
- 支持选择题号，自动排列
- 输入格式参考：`{"1": "A", "2": "D", "3": "C", ...}`
- 点击保存后调用 `POST /api/papers/{id}/answer-key`
- 这是自动批改的前置条件，OCR 识别后需要用它对比

### 🟡 1.4 编辑试卷
```
PapersView.vue 增加编辑功能
```
- 每行增加"编辑"按钮
- 复用创建试卷的表单，预填当前数据
- 调用 `PUT /api/papers/{id}`

### 🟡 1.5 删除试卷
```
PapersView.vue 增加删除功能
```
- 每行增加"删除"按钮（红色，带二次确认弹窗）
- 调用 `DELETE /api/papers/{id}`

### 🟢 1.6 试卷详情页
```
新增 PaperDetailView.vue
```
- 点击试卷名称进入详情页
- 展示：试卷信息 + 答案查看 + 已批改学生列表

---

## 二、教师端 — 批阅管理（🔴 体验优化）

### ✅ 2.1 待批阅列表
- 表格：学号/姓名/试卷/科目/AI得分/状态/操作
- 状态：已完成（修复过 res.data → res 问题）

### ✅ 2.2 批阅工作区
- 左右分栏：答卷图片 + 批改工具
- OCR 识别 + 自动批改 + 提交成绩
- 状态：已完成

### 🟡 2.3 评语提交（评语未入库）
```
MarkingView.vue 提交时带上 comment 字段
```
- 当前评语只存在于组件变量中，提交成绩时丢了
- 修改 submitScore 调用，增加 `comment` 参数
- 后端也需要对应支持（见后端清单 2.4）

### 🟡 2.4 批量批阅模式
```
新增批量批阅功能
```
- 在待批阅列表顶部加"批量批阅"按钮
- 选中多份答卷 → 一键 OCR 识别 + 自动批改
- 显示批量进度条

### 🟡 2.5 批阅状态颜色优化
```
用不同颜色区分：待批阅(灰) → AI批阅中(蓝) → 待审核(橙) → 已完成(绿)
```
- 在列表和详情中都体现

### 🟢 2.6 答卷图片放大查看
```
点击答卷图片弹出全屏预览
```
- 支持鼠标滚轮缩放
- 支持拖拽移动
- 高亮显示被识别到的区域

---

## 三、教师端 — 学情分析（🟡 数据增强）

### ✅ 3.1 成绩分布柱状图
- Chart.js 柱状图，按分数段统计
- 状态：已完成

### ✅ 3.2 成绩排名表
- 排名/学号/姓名/成绩/得分率/等级
- 状态：已完成

### ✅ 3.3 考试概览指标
- 平均分/最高分/最低分/人数
- 状态：已完成

### 🟡 3.4 多试卷对比
```
新增"对比分析"功能
```
- 选择两场或多场考试，并排展示对比图表
- 看出整体进步/退步趋势

### 🟢 3.5 导出成绩单
```
导出为 Excel 或打印
```
- "导出 Excel"按钮
- 前端生成 CSV → 浏览器下载

---

## 四、学生端（🟢 增强体验）

### ✅ 4.1 学生主页
- 统计卡片 + 最近成绩 + 快捷入口
- 状态：已完成

### ✅ 4.2 成绩查询
- 科目筛选 + 成绩表格 + 得分率进度条
- 状态：已完成

### ✅ 4.3 成绩趋势
- 科目标签 + 折线图 + 历次成绩明细
- 状态：已完成

### 🟢 4.4 成绩详情弹窗
```
点击某次成绩 → 弹出详情弹窗
```
- 显示：考试信息 + 得分明细 + 班级排名 + 教师评语

### 🟢 4.5 消息通知
```
顶部通知铃铛
```
- 新成绩发布时通知学生
- 目前可以先做一个"轮询检查有无新成绩"的简单版本

---

## 五、通用优化（🔴 重要）

### 🔴 5.1 删除冗余文件
```
删除 src/mian.js（main.js 的旧版本，未加载 CSS）
```
- 路径：`zhipi-frontend/src/mian.js`
- 这是个拼写错误的重复文件，遗留问题

### 🟡 5.2 全局加载状态
```
路由切换时显示全局 loading
```
- 使用 NProgress 或简单的顶部进度条
- 提升体验

### 🟡 5.3 错误处理统一优化
```
所有 API 调用加 try/catch，统一错误提示
```
- 网络断线 → 提示"网络连接失败"
- 401 → 提示"登录过期，请重新登录"并跳转登录页
- 500 → 提示"服务器错误，请联系管理员"

### 🟡 5.4 响应式适配
```
移动端/平板优化
```
- 教师端侧边栏 → 折叠式汉堡菜单
- 数据表格 → 卡片式布局
- 图表 → 全宽展示

### 🟢 5.5 深色模式
```
新增主题切换（浅色/深色/跟随系统）
```
- toggle 按钮放在顶部导航栏
- 用 CSS 变量实现，不需要改组件

### 🟢 5.6 无障碍优化
```
WCAG 2.1 AA 级别
```
- 按钮加 aria-label
- 颜色对比度检查
- 键盘导航支持

---

## 六、API 层完善（🟡）

### 🟡 6.1 补充缺失 API 调用
```
在 api/index.js 中新增：
```
- `paperApi.update(paperId, data)` → PUT
- `paperApi.delete(paperId)` → DELETE
- `paperApi.saveAnswerKey(paperId, answers)` → POST
- `paperApi.getDetail(paperId)` → GET
- `paperApi.submitScoresBatch(paperId, scores)` → POST
- `authApi.changePassword(oldPwd, newPwd)` → PUT

### 🟡 6.2 请求重试机制
```
网络不稳定时自动重试（最多3次）
```
- Axios 拦截器中实现
- 指数退避策略

---

## 📊 进度总览

| 模块 | 总数 | 已完成 | 待完成 |
|:---|:---|:---|:---|
| 试卷管理 | 6 | 2 | 4 |
| 批阅管理 | 5 | 2 | 3 |
| 学情分析 | 5 | 3 | 2 |
| 学生端 | 5 | 3 | 2 |
| 通用优化 | 6 | 0 | 6 |
| API 层 | 2 | 0 | 2 |
| **合计** | **29** | **10** | **19** |

---

## 🚀 建议开发顺序

```
第一轮（核心）      第二轮（重要）        第三轮（完善）
─────────────────  ─────────────────    ─────────────────
1.3 标准答案录入    5.1 删除冗余文件       2.6 图片全屏预览
2.3 评语提交        1.4 编辑试卷          3.4 多试卷对比
6.1 API 层补充      1.5 删除试卷          3.5 导出成绩单
                   2.4 批量批阅          4.4 成绩详情弹窗
                   5.3 错误统一处理       4.5 消息通知
                   5.4 响应式适配        5.5 深色模式
                                        5.6 无障碍
                                        5.2 全局 loading
```
